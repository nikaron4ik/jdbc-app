services:
  mysql:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: my_app_db
    ports:
      - "3306:3306"
    volumes:
      -  mysql_data:/var/lib/mysql

volumes:
  mysql_data: